style
  include:stylus index.styl    

if banner
  video(autoplay muted loop preload = "auto")
    source(src = banner.video.webm type = "video/webm")
    source(src = banner.video.mp4 type = "video/mp4")
  img.active(src = banner.image.png)
  
  h2!= banner.heading
  
